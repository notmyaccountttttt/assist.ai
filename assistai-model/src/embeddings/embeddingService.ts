// assistai-model/src/embeddings/embeddingService.ts
// TODO: Generate embeddings via Ollama nomic-embed-text
// - embed(text: string) → number[]
// - embedBatch(texts: string[]) → number[][]
export { }
